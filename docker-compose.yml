# COF - Backend API
# Uses the centralized PostgreSQL from Infra

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cof_api
    restart: unless-stopped

    environment:
      PORT: 3001
      DATABASE_URL: postgresql://${POSTGRES_USER:-cof}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-cof}
      NODE_ENV: ${NODE_ENV:-production}
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-30d}
      FRONTEND_URL: ${FRONTEND_URL:-https://jdr.francony.fr}

    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  default:
    name: cof_network
    external: true
